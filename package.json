{
    "$schema": "https://www.schemastore.org/package.json",
    "name": "dinners",
    "workspaces": [
        "frontend",
        "eslint-config-zed",
        "components"
    ],
    "dependencies": {
        "mprocs": "^0.7.3"
    },
    "scripts": {
        "dev": "mprocs",
        "lint": "bun run --filter='*' lint",
        "lint:fix": "bun run --filter='*' lint"
    }
}
